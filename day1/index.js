import { puzzle } from "./puzzle";

// create a function that maps over puzzle data
    // split each entry in the array
    // condition to remove all letters and join numbers together
    // return new array

// map over new numbers array
    // condition to only keep first and last digit in each item of the array
    // return these two numbers

// use reducer to sum all these numbers
    // return final sum value